<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sshtori"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Sshtori">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.Sshtori" />

        <activity
            android:name=".devices.DeviceHubActivity"
            android:exported="false"
            android:theme="@style/Theme.Sshtori" />

        <activity
            android:name=".devices.DeviceScanActivity"
            android:exported="false"
            android:theme="@style/Theme.Sshtori" />

        <activity
            android:name=".devices.DeviceDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Sshtori" />

        <activity
            android:name=".WiFiSetupActivity"
            android:exported="false"
            android:parentActivityName=".devices.DeviceHubActivity" />

    </application>

</manifest>